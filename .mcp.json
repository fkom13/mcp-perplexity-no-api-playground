{
  "tools": [
    {
      "name": "perplexity_playground_query",
      "description": "Interroge le playground Perplexity via Puppeteer. Paramètres : prompt, modèle (sonar-pro par défaut).",
      "parameters": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "description": "Prompt à envoyer."
          },
          "model": {
            "type": "string",
            "description": "Modèle à utiliser (parmi [r1-1776, sonar-pro, sonar, sonar-reasoning-pro, sonar-reasoning]).",
            "default": "sonar-pro"
          }
        },
        "required": ["prompt"]
      },
      "responses": {
        "type": "object",
        "properties": {
          "prompt": {"type": "string"},
          "model": {"type": "string"},
          "result": {"type": "string"}
        }
      }
    }
  ]
}
